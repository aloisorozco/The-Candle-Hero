using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
TODO (nice to have for thu):
-add health bonuses, timeToStop bonuses, etc. specified in story docs
-add items/gameobjects in level scene and when those are picked up by player, bonus is given
 */

//this class will contain bonuses for player
public class Bonuses : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
